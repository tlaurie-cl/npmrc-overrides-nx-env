{
  "name": "example",
  "scripts": {
    "nx": "nx"
  },
  "devDependencies": {
    "nx": "20.5.0"
  },
  "nx": {
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "node build.js"
        }
      }
    }
  }
}